/* eslint-disable */
// STUB — coin state moves to React Query

export const setCoinBalance = (_v: any) => ({ type: 'stub/setCoinBalance' })

export default function coinReducer(state = {}, _action: any) {
  return state
}
